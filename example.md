# h1 Heading: The Main Title

This is a standard paragraph of text. Paragraphs are separated by a blank line.

## h2 Heading: Text Styling

* This text will be **bold** to emphasize important words.
* This text will be *italic* to denote slight emphasis.
* This text will have a ~~strikethrough~~ to show removed items.
* You can also combine them to make text ***bold and italic***.

---

### Blockquotes
> This is a blockquote element. It is commonly used to display quotes, multi-line notes, or highlighted tips within documentation.

### Tables

| Tool Name | Purpose | Ease of Use |
| :--- | :--- | :---: |
| Markdown | Fast documentation | Excellent |
| HTML | Web structure | Moderate |

### Code Blocks

You can display `inline code` wrapped in single backticks. For larger blocks of code, use triple backticks with syntax highlighting:

```javascript
// A simple JavaScript function example
function greetUser(name) {
    console.log("Hello, " + name + "!");
}
greetUser("Developer");
```

```python
# A simple Python example
def calculate_total(price, tax):
    return price + (price * tax)
```

### Task Lists
- [x] Create a sample Markdown file
- [x] Include standard headings and lists
- [ ] Add advanced elements like tables
